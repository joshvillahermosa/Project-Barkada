node {
    sh 'cd ..'

    nvm(nvmInstallURL: 'https://raw.githubusercontent.com/creationix/nvm/v0.33.2/install.sh', nvmIoJsOrgMirror: 'https://iojs.org/dist', nvmNodeJsOrgMirror: 'https://nodejs.org/dist', version: '8.8.1') {
        stage('Install') {
            checkout scm
            sh 'npm install'
        }

        stage('@TODO CI-Test') {
            sh 'npm run test'
        }

        stage('@TODO Build') {
            
        }
    }
    
    stage('@TODO Archive') {
        // archiveArtifacts 'test/unit/coverage/**/*.*'
        // archiveArtifacts 'dist/**/*.*'
        // publishHTML([allowMissing: true, alwaysLinkToLastBuild: false, keepAll: true, // reportDir: 'test/unit/coverage/lcov-report', reportFiles: 'index.html', reportName: 'Coverage', reportTitles: ''])
    }

    stage('@TODO Upload to S3') {
        
    }
}
